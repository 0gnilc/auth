# Separate UI action visibility from API enforcement

Represent administrative actions as Button Menus whose access codes control frontend visibility, while backend permissions independently enforce the corresponding operations. Matching visibility and permission grants avoid unusable controls without treating a hidden control as a security boundary.

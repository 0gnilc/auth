# Protect built-in RBAC resources

Persist built-in status on roles, permissions, and menus and reject administrative mutation or deletion of those resources; built-in role permission and menu grants are system-maintained as well. Assignment expresses use rather than ownership, so assigning an ordinary resource to a built-in role never changes the resource's identity or mutability.

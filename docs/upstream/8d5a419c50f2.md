---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: 8d5a419c50f2a16504c7222b5c6caf4c8f65c21b
upstream_date: 2026-07-23
status: partial
reviewed_at: 2026-07-30
local_commits:
  - 180ba41eff33a6d15af74ed4057c5aaa41851d45
  - 3508304cbc556211cc9177ed437fe8ea314b6c69
scope:
  - apps/admin
  - packages/@core/ui-kit/form-ui
  - packages/effects/layouts
  - packages/effects/plugins
  - packages/locales
---

# 上游提交：Merge branch 'vbenjs:main' into main

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/8d5a419c50f2a16504c7222b5c6caf4c8f65c21b>

## 影响评估

该提交同时包含 typed form codecs、vxe-table 适配和布局下拉菜单完善。布局部分补齐主题、时区、全屏、通知等放入用户菜单后的真实交互，改进拖拽项选择器并暴露通知和时区组件控制接口。

## 同步决定

状态标记为 `partial`。布局与偏好设置最终态已在前一批同步；typed form codecs 和当前项目保留的 vxe-table 适配已在连续表单批次同步。当前没有待后续同步内容，多 UI 应用、文档站和 playground 示例永久过滤。

- 已同步内容：用户下拉菜单交互、通知与时区控制、拖拽选择器最终行为、typed form codecs 和当前项目仍使用的 vxe-table 适配。
- 待后续同步内容：无。
- 永久过滤内容：其他 UI 应用、文档站和 playground 示例。

## 本地调整

- UserDropdown 增加通知 props/events，与 `apps/admin/src/layouts/basic.vue` 的本地通知列表、未读状态和清理/已读/删除操作共用同一数据源。
- BasicLayout 传递本地 avatar、nickname 和 logout；保留用户描述、个人资料菜单、GitHub/帮助入口、水印、登录过期弹窗和认证流程。
- 保留 `lockScreen: false`，将本地默认退出位置设为 `user-dropdown`。
- 保留主题重置时同步组件 DOM 状态的本地修复。
- 引入 raw value 与 submission value 的双类型表单 codecs，并将本地 Admin 表单适配器接入该类型边界。
- vxe-table 保留本地组件注册和查询约定，同时同步上游 API、泛型与表单重置行为。

## 待后续同步内容

无。

## 永久过滤内容

- 上游其他 UI 应用、文档站、playground 和 `.gitignore` 变更。

## 验证

- `pnpm run test:admin` 通过，共 50 个测试文件、369 个测试。
- `pnpm --filter=@app/admin run typecheck`、`pnpm run lint` 和 `pnpm run build:admin` 通过。
- 构建仅出现第三方 `@vueuse/core` PURE annotation 警告，不影响成功产物。
- `git diff --check` 通过。
- typed codecs、表单运行时及 vxe-table 连续迁移验证通过：表单专项 8 个文件、82 个测试，Admin 60 个文件、457 个测试，Admin 类型检查、`pnpm run check`、`pnpm run lint` 和两组性能基准均通过。

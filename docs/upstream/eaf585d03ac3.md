---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: eaf585d03ac3094b6324efcff589753ddd512412
upstream_date: 2026-07-23
status: partial
reviewed_at: 2026-07-30
local_commits:
  - 3508304cbc556211cc9177ed437fe8ea314b6c69
scope:
  - apps/admin
  - packages/@core/ui-kit/form-ui
  - packages/@core/ui-kit/shadcn-ui
  - pnpm-workspace.yaml
---

# 上游提交：完善 Zod 4 表单运行时与数组字段

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/eaf585d03ac3094b6324efcff589753ddd512412>

## 影响评估

该合并提交改进表单边界、数组字段焦点、表单 API、运行时类型和性能，并补充大量测试。它建立在 Zod 4 与 TanStack Form 迁移之上，当前项目的登录、密码、管理员、RBAC、菜单和动态国际化表单均会受影响。

## 同步决定

状态标记为 `partial`。当前项目适用的表单边界、数组字段焦点、运行时、类型和性能改进已随连续表单迁移完成；playground 示例、日期范围工具和未保留应用的演示业务表单永久过滤。当前没有待后续同步内容。

### 已同步内容

- 表单 raw/submission value 边界、codecs、运行时字段、依赖计算、规则注册和 Zod 4 schema 处理。
- 数组字段增删、焦点恢复、校验与类型推导，以及兼容性、集成、类型和性能测试。
- 当前 Admin 表单适配器和业务表单迁移。

### 待后续同步内容

无。

### 永久过滤内容

- playground 表单示例、日期范围工具和当前项目未保留应用的演示业务表单。

## 本地调整

- 共享表单实现同步到 `b04d728249e1` 的最终状态，避免保留中间提交随后被覆盖的运行时代码。
- `UnwrappableZodType` 因 CSpell 规则更名为语义等价的 `ZodTypeWithUnwrap`，行为不变。
- 本地 Admin 的管理员、菜单、动态国际化、密码与个人设置表单按新 API 适配，同时保留权限动作、未保存变更保护和 Message Key 独立持久化边界。

## 验证

- 冻结 lockfile 的 pnpm 11.16 安装通过。
- 表单专项测试通过，共 8 个文件、82 个测试；两组表单性能基准通过。
- Admin 测试通过，共 60 个文件、457 个测试；Admin 类型检查通过。
- `pnpm run check`、`pnpm run lint`、`pnpm run build:admin` 和 `git diff --check` 通过。
- `check` 仅保留上游共享循环依赖和既有未使用依赖警告；构建仅保留第三方 `@vueuse/core` PURE annotation 警告。

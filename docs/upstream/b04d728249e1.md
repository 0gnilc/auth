---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: b04d728249e1dbc6ce5ab1beb33835a92583feeb
upstream_date: 2026-07-28
status: synced
reviewed_at: 2026-07-30
local_commits:
  - 3508304cbc556211cc9177ed437fe8ea314b6c69
scope:
  - packages/@core/ui-kit/form-ui
---

# 上游提交：修复 Zod 4 数组规则被错误 unwrap

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/b04d728249e1dbc6ce5ab1beb33835a92583feeb>

## 影响评估

该提交让迁移后的表单 helper 正确识别 Zod 4 的 `ZodArray`，避免误用 `unwrap()` 丢失数组外壳。连续表单迁移已同时引入其前置的 Zod 4 表单实现。

## 同步决定

状态标记为 `synced`。数组规则 `unwrap()` 防护已随连续表单迁移完整同步，没有过滤内容或待后续同步项。

## 本地调整

- `getSchemaDefaultValue()` 仅对支持 `unwrap()` 且不是 `ZodArray` 的 schema 解包，数组 schema 保留数组外壳和默认值语义。
- 上游类型名 `UnwrappableZodType` 因 CSpell 规则更名为语义等价的 `ZodTypeWithUnwrap`，运行逻辑不变。

## 验证

- Zod 4 schema 专项测试通过，覆盖数组规则不被错误解包。
- 表单专项测试通过，共 8 个文件、82 个测试；Admin 测试通过，共 60 个文件、457 个测试。
- Admin 类型检查、`pnpm run check`、`pnpm run lint`、`pnpm run build:admin` 和 `git diff --check` 通过。

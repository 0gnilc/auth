---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: 81f69d797d15c502356e6a7788fee617effe3462
upstream_date: 2026-07-22
status: partial
reviewed_at: 2026-07-29
local_commits:
  - 180ba41eff33a6d15af74ed4057c5aaa41851d45
scope:
  - packages/@core/base/shared
  - packages/@core/preferences
  - packages/effects/layouts
  - pnpm-lock.yaml
---

# 上游提交：Merge branch 'vbenjs:main' into main

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/81f69d797d15c502356e6a7788fee617effe3462>

## 影响评估

该提交的核心修复是让偏好差异比较对数组顺序敏感，使小部件拖拽后的顺序能够被保存；同时调整 Header 顺序、SortableJS 依赖和 playground 退出映射，并夹带根依赖及 lockfile 刷新。

## 同步决定

状态标记为 `partial`。完整同步共享 diff、偏好计算和布局顺序修复，选择性同步布局包依赖；过滤 playground 映射和根依赖批量更新。

## 本地调整

- 保留原 `diff` 的数组无序比较语义，新增 `diffStrict` 供偏好设置使用，并补充数组顺序、元素和嵌套对象测试。
- 将 Header 的 refresh 顺序与 `widget.order` 默认值对齐。
- 为 `packages/effects/layouts/package.json` 增加 SortableJS 运行时及类型依赖，lockfile 仅更新该 importer。

## 过滤内容

- `playground/src/layouts/basic.vue` 的退出映射，等价行为已手工映射到本地 `apps/admin`。
- 根 `package.json` 和全量 `pnpm-lock.yaml` 依赖刷新。

## 验证

- shared/preferences 定向测试通过；完整前端测试共 50 个文件、369 个测试通过。
- `pnpm --filter=@app/admin run typecheck`、`pnpm run lint` 和 `pnpm run build:admin` 通过。
- `git diff --check` 通过。

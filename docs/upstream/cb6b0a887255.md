---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: cb6b0a887255139db58b262b09a15cc87e4299e8
upstream_date: 2026-07-24
status: synced
reviewed_at: 2026-07-29
local_commits:
  - e36a5c771eb766b23aad5c07b18a25c4ddf0f5cb
scope:
  - packages/@core/ui-kit/shadcn-ui
---

# 上游提交：限制下拉子菜单宽度

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/cb6b0a887255139db58b262b09a15cc87e4299e8>

## 影响评估

该提交只修改 `DropdownMenuSubContent.vue`，使用 Reka UI 提供的可用宽度变量限制子菜单最大宽度，避免长内容或窄视口下子菜单超出可见区域。当前文件的目标类字符串与上游第一父版本一致。

## 同步决定

状态标记为 `synced`。上游提交的一行变更完整适用于当前项目，没有范围外文件或过滤内容。

## 本地调整

无。仅增加 `max-w-(--reka-dropdown-menu-content-available-width)`。

## 验证

- `pnpm run test:admin` 通过，共 50 个测试文件、369 个测试。
- `pnpm --filter=@app/admin run typecheck`、`pnpm run lint` 和 `pnpm run build:admin` 通过。
- `git diff --check` 通过。

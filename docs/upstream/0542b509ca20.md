---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: 0542b509ca20da477d003f7c878692287d8f6875
upstream_date: 2026-07-23
status: partial
reviewed_at: 2026-07-29
local_commits:
  - 3c043039eff683213a87e818e7b18eecfa5daa5f
scope:
  - package.json
  - pnpm-lock.yaml
  - pnpm-workspace.yaml
---

# 上游提交：集中升级 pnpm 与前端依赖

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/0542b509ca20da477d003f7c878692287d8f6875>

## 影响评估

该提交集中刷新 pnpm、workspace catalog 和 lockfile，包含当前项目仍使用的运行时与工具依赖，也包含已删除应用、文档站、mock 和其他 UI 框架的依赖。

## 同步决定

状态标记为 `partial`。pnpm 和当前 workspace 实际使用的 22 个 catalog 依赖已升级到上游目标声明，并基于当前精简 workspace 重新生成 lockfile；未使用或属于已删除应用的依赖永久过滤。

### 已同步内容

- 将 `packageManager` 从 `pnpm@11.7.0` 升级到 `pnpm@11.16.0`。
- 将当前 workspace 实际引用的 22 个 catalog 依赖升级到上游目标版本，覆盖图标、国际化、VXE Table、构建、类型检查、测试与 lint 工具链。
- 使用 pnpm 11.16 为当前 40 个 workspace importer 重新解析 `pnpm-lock.yaml`，并同步更新本地 `node_modules`。

### 待后续同步内容

无。

### 永久过滤内容

- `antdv-next`；当前项目未保留对应应用和依赖。
- `@tanstack/vue-query`；当前 catalog 虽保留该声明，但 workspace 没有实际引用，本次不升级且不写入 lockfile。
- 上游已删除应用、文档站、mock 和其他 UI 框架产生的依赖及 lockfile 条目。

## 本地调整

- 保留当前精简后的 workspace、Node.js engine、本地脚本和未包含在本提交中的 catalog 版本，不复制上游整份清单。
- 当前部分依赖起点早于上游父提交，因此直接升级到本提交的目标声明；lockfile 按 caret 范围解析为同步时可用的兼容版本。
- 不提前引入 Zod 4、`@tanstack/vue-form` 或相关表单迁移依赖。
- `node_modules` 已在本地更新但不纳入 Git；可复现依赖状态由三个依赖声明及锁文件保存。

## 验证

- pnpm 11.16 供应链策略检查通过。
- `CI=1 corepack pnpm@11.16.0 install --frozen-lockfile` 通过，本地依赖与 lockfile 一致。
- 机器校验确认 22 个 catalog specifier 与 lockfile 声明一致、两个过滤依赖未进入 lockfile、40 个 workspace importer 保持不变。
- `pnpm run check` 通过；保留现有循环依赖与未使用依赖警告。
- `pnpm run test:admin` 通过，共 53 个测试文件、389 个测试。
- `pnpm run lint` 通过。
- `pnpm run build:admin` 通过；仅保留第三方 `@vueuse/core` PURE annotation 警告。
- 额外的 `pnpm run publint` 仍报告未构建包缺少 `dist` 和现有 `sideEffects` 建议；使用升级前的 publint 0.3.21 复测结果相同，不属于本次升级回归。
- `git diff --check` 通过。

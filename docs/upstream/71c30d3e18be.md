---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: 71c30d3e18be12ef752aff8223cd4d2438d4a199
upstream_date: 2026-07-14
status: synced
reviewed_at: 2026-07-29
local_commits:
  - 1e213746e8d45408a4adb0066478282315c2abbe
scope:
  - packages/effects/layouts
---

# 上游提交：Merge branch 'vbenjs:main' into main

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/71c30d3e18be12ef752aff8223cd4d2438d4a199>

## 影响评估

该提交将头部的主题、语言和时区按钮从偏好设置按钮的位置判断中移出，使三个按钮只由各自的 widget 开关控制。当前项目保留了相同的共享布局实现；同步前的文件与该提交的第一父版本完全一致，因此问题同样存在。

## 同步决定

状态标记为 `synced`。该修复适用于当前项目，且不涉及已删除的上游应用、示例或服务端模块。

## 本地调整

在 `packages/effects/layouts/src/basic/header/header.vue` 中保留偏好设置按钮原有的位置判断，同时让 `themeToggle`、`languageToggle` 和 `timezone` 分别独立决定对应头部按钮是否显示。文件内容与上游提交一致，无额外本地偏差。

## 验证

- 已校验目标文件与上游提交中的版本完全一致。
- `git diff --check` 通过。
- `pnpm --filter=@app/admin run typecheck` 通过。

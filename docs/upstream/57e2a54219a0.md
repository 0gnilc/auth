---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: 57e2a54219a0f43eb5faf8747fab75db9358d70c
upstream_date: 2026-07-24
status: partial
reviewed_at: 2026-07-29
local_commits:
  - 0f4e488738dcfb031e761f6867321625fad28c4d
scope:
  - packages/@core/ui-kit/shadcn-ui
---

# 上游提交：为表单注入 Symbol 补充描述

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/57e2a54219a0f43eb5faf8747fab75db9358d70c>

## 影响评估

该提交为迁移后的表单注入键补充 Symbol 描述。当前项目仍处于迁移前结构，只有 `FORM_ITEM_INJECTION_KEY`，上游目标文件还包含迁移后新增的注入键。

## 同步决定

状态标记为 `partial`。当前已有的 `FORM_ITEM_INJECTION_KEY` 已同步 Symbol 描述；迁移后才会出现的 `FORM_FIELD_INJECTION_KEY` 不提前引入，等待 Zod 4 与 TanStack Form 连续迁移。

### 已同步内容

- 将现有 `FORM_ITEM_INJECTION_KEY` 从无描述的 `Symbol()` 改为 `Symbol('FORM_ITEM_INJECTION_KEY')`。

### 待后续同步内容

- 为迁移后新增的 `FORM_FIELD_INJECTION_KEY` 设置同名 Symbol 描述；该注入键将在连续表单迁移引入时一并处理。

### 永久过滤内容

无。

## 本地调整

- 保留当前迁移前的表单注入结构，不为满足上游目标文件而提前新增 `FormFieldContext` 或 `FORM_FIELD_INJECTION_KEY`。
- Symbol 描述只改善 Vue DevTools 和错误诊断中的可读性，不改变注入键的唯一性与依赖注入行为。

## 验证

- 已确认当前项目没有 `FORM_FIELD_INJECTION_KEY` 引用，未越过本次同步范围。
- `pnpm --filter=@app/admin run typecheck` 通过。
- `pnpm run test:admin` 通过，共 53 个测试文件、389 个测试。
- `pnpm run lint` 通过。
- `pnpm run build:admin` 通过；仅保留第三方 `@vueuse/core` PURE annotation 警告。
- `git diff --check` 通过。

---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: ebc0f23e644e8af98029b1c137591046338a393c
upstream_date: 2026-07-22
status: partial
reviewed_at: 2026-07-29
local_commits:
  - 180ba41eff33a6d15af74ed4057c5aaa41851d45
scope:
  - packages/effects/layouts
---

# 上游提交：Merge branch 'vbenjs:main' into main

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/ebc0f23e644e8af98029b1c137591046338a393c>

## 影响评估

该提交是 Zod 4 与 TanStack Form 的大规模迁移，共修改 81 个文件。与本轮布局批次直接相关的独立修复只有 SortableJS 实例在组件卸载时的清理。

## 同步决定

状态标记为 `partial`。本轮只同步可独立验证的拖拽实例生命周期清理；表单运行时、Zod 4、各 UI 适配器、vxe-table 和示例迁移留在后续连续表单批次。

## 本地调整

- 在 `draggable-list.vue` 卸载时销毁 SortableJS 实例并清空引用，避免重复挂载监听器和保留失效 DOM 引用。
- 未改动本地 Admin 的登录、密码、管理员、RBAC、菜单和动态国际化表单。

## 过滤内容

- `packages/@core/ui-kit/form-ui` 的 Zod 4、TanStack Form 运行时和兼容层。
- 上游多 UI 应用、文档站、playground、vxe-table 和 workspace 依赖迁移。

## 验证

- `pnpm run test:admin`、`pnpm --filter=@app/admin run typecheck`、`pnpm run lint` 和 `pnpm run build:admin` 通过。
- `git diff --check` 通过。

---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: ebc0f23e644e8af98029b1c137591046338a393c
upstream_date: 2026-07-22
status: partial
reviewed_at: 2026-07-30
local_commits:
  - 180ba41eff33a6d15af74ed4057c5aaa41851d45
  - 3508304cbc556211cc9177ed437fe8ea314b6c69
scope:
  - apps/admin
  - packages/@core/ui-kit/form-ui
  - packages/@core/ui-kit/shadcn-ui
  - packages/effects/layouts
  - packages/effects/plugins
  - pnpm-workspace.yaml
---

# 上游提交：Merge branch 'vbenjs:main' into main

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/ebc0f23e644e8af98029b1c137591046338a393c>

## 影响评估

该提交是 Zod 4 与 TanStack Form 的大规模迁移，共修改 81 个文件，同时包含 SortableJS 生命周期清理、表单适配器和示例变更。当前项目保留共享表单、Shadcn 表单上下文、Element Plus Admin、vxe-table 和布局组件。

## 同步决定

状态标记为 `partial`。拖拽实例清理已在布局批次同步；Zod 4、TanStack Form、Shadcn 表单上下文及当前项目保留的 Admin 和 vxe-table 适配已在连续表单批次同步到上游目标最终状态。当前没有待后续同步内容。

- 已同步内容：SortableJS 组件卸载清理；Zod 4 与 TanStack Form 运行时；Shadcn `FormField` 上下文；当前 Admin 与 vxe-table 适配；相关 workspace 依赖。
- 待后续同步内容：无。
- 永久过滤内容：其他 UI 应用、文档站和 playground 示例。

## 本地调整

- 在 `draggable-list.vue` 卸载时销毁 SortableJS 实例并清空引用，避免重复挂载监听器和保留失效 DOM 引用。
- 将共享表单从 Vee Validate 迁移到 TanStack Form/Store，并升级到 Zod 4 与 `zod-defaults`；依赖和 lockfile 仅保留当前 workspace 实际需要的条目。
- 将上游 Element Plus 行为映射到 `apps/admin`，适配管理员、菜单、动态国际化、密码与个人设置表单；保留权限动作、未保存变更保护和 Message Key 独立持久化边界。
- 当前项目使用的 vxe-table API、泛型和表单处理同步到最终状态。

## 待后续同步内容

无。

## 永久过滤内容

- 上游其他 UI 应用、文档站和 playground 示例。

## 验证

- `pnpm run test:admin`、`pnpm --filter=@app/admin run typecheck`、`pnpm run lint` 和 `pnpm run build:admin` 通过。
- `git diff --check` 通过。
- 冻结 lockfile 的 pnpm 11.16 安装通过。
- 连续表单迁移验证通过：表单专项 8 个文件、82 个测试，Admin 60 个文件、457 个测试，`pnpm run check` 和两组表单性能基准均通过。

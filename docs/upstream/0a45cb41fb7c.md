---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: 0a45cb41fb7ca949c45dbfbb4fd39d8a9b48c9cf
upstream_date: 2026-07-23
status: partial
reviewed_at: 2026-07-29
local_commits:
  - c38353f8fce37004d8d7b359f877958ecdf3162c
scope:
  - apps/admin
  - internal/tailwind-config
  - internal/vite-config
---

# 上游提交：增加 CSS Layer 与组件库样式适配

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/0a45cb41fb7ca949c45dbfbb4fd39d8a9b48c9cf>

## 影响评估

该提交新增 Vite CSS Layer 插件，调整 Tailwind theme layer，并修改多个 UI 应用的组件适配。当前 Admin 使用 Element Plus，相关 CSS Layer 和 InputNumber 适配需要映射到 `apps/admin`，不能直接复制上游应用文件。

## 同步决定

状态标记为 `partial`。共享 Tailwind/Vite 能力和当前 Admin 使用的 Element Plus 行为已经完成映射；Ant Design Vue、Ant Design Vue Next、Naive UI、TDesign 和 playground 相关文件永久过滤。

### 已同步内容

- 在 Tailwind 入口首次导入 `tailwindcss` 前声明 `theme`、`base`、各 UI 库、`components` 和 `utilities` 的级联层顺序。
- 新增并导出通用 `viteCssLayerPlugin`，按包名匹配 CSS 模块并包装到指定的 `@layer`。
- 将上游 `apps/web-ele/vite.config.ts` 的 Element Plus CSS Layer 行为映射到 `apps/admin/vite.config.ts`，把 Element Plus 样式放入 `@layer el`。
- 将上游 Element Plus InputNumber 适配映射到当前 Admin，移除内联 `width: 100%`，允许 Tailwind 宽度工具类按级联层覆盖组件样式。

### 待后续同步内容

无。

### 永久过滤内容

- `apps/web-antd` 和 `apps/web-antdv-next` 的应用配置与组件适配；当前项目未保留这些应用。
- `apps/web-naive` 和 `apps/web-tdesign` 的应用配置与组件适配；当前项目未保留这些应用。
- `playground` 的 CSS Layer 演示配置；当前项目不保留上游演示应用。

## 本地调整

- 使用当前项目的 `apps/admin` 替代上游 `apps/web-ele` 作为 Element Plus 行为的落点。
- 保留 Admin 现有代理目标 `http://localhost:3666` 和其他本地 Vite 配置。
- 保留当前 Admin 组件适配器中的 `I18nMessageInput` 等本地扩展，只调整 `InputNumber` 的默认内联样式。
- 共享的 Tailwind theme 和 Vite CSS Layer 插件实现与上游提交一致；Tailwind theme 中的 `@apply` 顺序调整随上游一并同步。

## 验证

- `pnpm --filter=@vben/vite-config run stub` 通过。
- `pnpm --filter=@app/admin run typecheck` 通过。
- `pnpm run test:admin` 通过，共 53 个测试文件、389 个测试。
- `pnpm run lint` 通过。
- `pnpm run build:admin` 通过；仅保留第三方 `@vueuse/core` PURE annotation 警告。
- 构建产物确认 `.el-input-number` 规则位于 `@layer el` 内，Tailwind `w-full` 工具类正常生成。
- CSS Layer 插件探针确认 Element Plus CSS 会被包装，非目标包 CSS 不受影响。
- `git diff --check` 通过。

---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: 5c1d31f680b99e24fdda7df6fd61f7d355b11ab2
upstream_date: 2026-07-22
status: partial
reviewed_at: 2026-07-29
local_commits:
  - 180ba41eff33a6d15af74ed4057c5aaa41851d45
scope:
  - packages/@core/preferences
  - packages/@core/ui-kit/shadcn-ui
  - packages/effects/layouts
  - packages/locales
  - pnpm-lock.yaml
---

# 上游提交：Merge branch 'vbenjs:main' into main

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/5c1d31f680b99e24fdda7df6fd61f7d355b11ab2>

## 影响评估

该提交修改 40 个文件，布局侧引入小部件拖拽排序、数组覆盖式偏好合并、全屏 tooltip、Popover trigger 修复和用户下拉菜单扩展；同时包含上游文档站、mock 服务、工具脚本、workspace catalog 与完整 lockfile 刷新。

## 同步决定

状态标记为 `partial`。同步当前项目所需的偏好数组语义、拖拽排序和布局组件改进；不接收与本轮无关的文档站、示例应用、mock 数据、lint/运行脚本及全量工具链升级。

## 本地调整

- 使用 `mergeWithArrayOverride` 更新偏好数组，加载旧缓存时清理 `widget.order` 重复项，避免数组在反复更新时持续膨胀。
- 新增 `draggable-list.vue`，在偏好抽屉中统一管理小部件顺序和显示位置。
- 保留本地主题重置时先同步 DOM 再写缓存的逻辑及其回归测试。
- 只为 `@vben/layouts` importer 增加 `sortablejs` 与 `@types/sortablejs`，保留本地 `@vue/test-utils`，未应用上游整份 lockfile 和 catalog 更新。

## 过滤内容

- `.github/config.yml`、`apps/backend-mock`、`apps/web-antd` 和 `docs/src`。
- `playground` 表单、菜单示例及本项目未保留的应用代码。
- `internal/lint-configs`、`scripts/turbo-run`、`scripts/vsh` 和根依赖批量升级。

## 验证

- `pnpm run test:admin` 通过，共 50 个测试文件、369 个测试。
- `pnpm --filter=@app/admin run typecheck` 通过。
- `pnpm run lint` 通过。
- `pnpm run build:admin` 通过。
- `git diff --check` 通过。

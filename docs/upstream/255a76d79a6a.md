---
upstream_repository: https://github.com/vbenjs/vue-vben-admin
upstream_commit: 255a76d79a6adc09006baf8edba31cfb825c1b5b
upstream_date: 2026-07-24
status: partial
reviewed_at: 2026-07-29
local_commits:
  - b0b86fca25337ab6ffdeb0f40ea58c5c34783e1b
scope:
  - packages/@core/ui-kit/shadcn-ui
---

# 上游提交：规范化 Tailwind 类名

上游提交：<https://github.com/vbenjs/vue-vben-admin/commit/255a76d79a6adc09006baf8edba31cfb825c1b5b>

## 影响评估

该提交规范化多个共享 Shadcn UI 组件的 Tailwind 类名，并修改一个 playground 表单数组示例。共享组件仍由当前 Admin 使用，类名调整可独立同步。

## 同步决定

状态标记为 `partial`。上游提交涉及的 11 个共享 Shadcn UI 组件已完整同步；当前项目未保留的 playground 表单数组示例永久过滤。

- 已同步内容：11 个共享 Shadcn UI 组件的 Tailwind 类名规范化。
- 待后续同步内容：无。
- 永久过滤内容：`playground/src/views/demos/form-array/index.vue`。

## 本地调整

- 将 10 个组件的三像素 ring 任意值类改为 Tailwind v4 规范类名。
- 将 Checkbox 的四像素圆角任意值类改为 `rounded-sm`。
- 将 CollapsibleParams 的一像素定位任意值类改为 `bottom-px`、`left-px` 和 `right-px`。
- 适用文件的 patch-id 与上游提交排除 playground 后的 patch-id 完全一致，无额外本地偏差。

## 验证

- `pnpm run test:admin` 通过，共 50 个测试文件、369 个测试。
- `pnpm --filter=@app/admin run typecheck` 通过。
- `pnpm run lint` 通过。
- `pnpm run build:admin` 通过；Tailwind v4 正确解析规范化类名，仅保留第三方 `@vueuse/core` PURE annotation 警告。
- `git diff --check` 通过。
